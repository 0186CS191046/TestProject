# TestProject
<!--  create virtual environment for run django ==> python -m venv venv -->
<!-- then run  .\venv\Scripts\Activate.ps1  for go inside virtual env 
then install django that env using ==>  python -m pip install django 
then run python manage.py runserver -->